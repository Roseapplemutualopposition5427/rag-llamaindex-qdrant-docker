{
  "schema_version": "1.0",
  "title": "Build a RAG System with LlamaIndex and Qdrant in Docker",
  "tools": [
    {"name": "docker", "version": ">=24.0", "check": "docker --version"},
    {"name": "docker compose", "version": ">=2.20", "check": "docker compose version"},
    {"name": "python", "version": ">=3.12", "check": "python3 --version"},
    {"name": "git", "version": ">=2.40", "check": "git --version"}
  ],
  "env": {
    "required_one_of": [
      {"name": "OPENAI_API_KEY", "description": "OpenAI API key for embeddings + LLM synthesis"},
      {"name": "OLLAMA_HOST", "description": "Ollama server URL (e.g. http://localhost:11434), with OLLAMA_MODEL set to bge-m3"}
    ]
  },
  "resources": {
    "disk_mb": 2000,
    "ram_mb": 2000
  },
  "estimated_duration_minutes": 30,
  "supported_platforms": ["linux", "macos"]
}
